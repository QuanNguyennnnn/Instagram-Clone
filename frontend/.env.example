# Frontend không cần biến môi trường khi chạy local
# Vite tự proxy /api → localhost:5000 qua vite.config.js
# Chỉ cần khi deploy production:
# VITE_API_URL=https://your-backend-domain.com
# VITE_SOCKET_URL=https://your-backend-domain.com
